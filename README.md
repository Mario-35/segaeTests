# segaeTests

premier point et pas des moindres car le temps perdu a aller à la peche aux informations est très voir trop chronophage.

IL NE SAGIT PAS DU SOURCE mais des fichier "webPackés" (cela est un soucis pour un maintient éventuel).

on accede pas au code directement les fichiers js sont en fait des objets qui sont ensuite injectés dans l'appli et les fichier js en clair comme rules.js, formulas.js sont en fait des données car il lance des fonction initialisant les propriétés du jeu.

le fichier config indique les fichiers permetant les clés pour les tests mais on voit aussi l'appel a un serveur vertex : https://vertx.io/ sous java dont le code n'est pas transmis ainsi que le fichier VertxClient.js qui contient la premiere ligne : Generated by Haxe 4.1.1 https://haxe.org/ là encore sans aucun code transmis.

Si le contrat de base indiquait que le source vous soit transmis ce n'est pas le cas.


## desobfuscation des fichiers

npx install -g uglify-js
npx uglify-js ugly.js -b --source-map "filename='ugly.js.map'" -o beauty.js



Autre soucis : dans l'index on vois que les fichiers js sont en fait des objects qui sont bindé au démarrage ca complique fortement le travail.

## Etats des lieux

le fichier config indique les fichiers permetant les clés pour les tests mais on voit aussi l'appel a un serveur vertex : https://vertx.io/ sous java dont le code n'est pas transmis 

le fichier formulas qui enregistre les formules.

soucis particulier : les fichiers excel sont géré par : SheetJS https://github.com/sheetjs/sheetjs, dans le code on reconnais bien la syntaxe sheet_to_json propre a cette librarie
## injection du test 
j'ai créer un fichier tests.js ou tout le process de test est mis en place

je charge ce fichier le plus tot dans index.html

j'ai reperé la classe css "ui-button ui-button-left ui-button-top" qui represente "Year 1" qui n'a aucune fonction j'appelle donc startTests pour demarrer les tests

```JS
            }, r.a.createElement("div", {
                className: "ui-button ui-button-left ui-button-top"
            }, r.a.createElement("span", {
                className: "label",
                onClick: function() { startTests()}
            }, oe.t("year"), " ", oe.yearNumber), r.a.createElement("div", {
                className: "separator"
            }), r.a.createElement("span"
```

maintenant en appuyant sur l'année les tests sont activés.

![Lancer les tests](./assets/launch%20test.png"Tests")